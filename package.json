{
  "name": "imspresentation",
  "version": "1.0.0",
  "description": "Real-time interactive presentation platform with live polls, quizzes, Q&A, and subtitles",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build",
    "start": "node server/src/index.js",
    "dev": "node server/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
